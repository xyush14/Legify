fastapi>=0.110
uvicorn[standard]>=0.27
anthropic>=0.39
pydantic>=2.6
python-dotenv>=1.0
deep-translator>=1.11      # free Hindi translation (no API key)

# legacy Streamlit version — only needed if running app.py
streamlit>=1.36
